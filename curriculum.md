# C# 게임 개발자 커리큘럼 (22주)

## 설계 기준
- **현재 수준**: Day 6 완료 (변수, 자료형, 조건문, 반복문, 배열 기본)
- **시작점**: Day 7 (배열 심화 / 연쇄 번개 문제)
- **학습 패턴**: 주간 목표제 (주 5개념, 유연한 일정, 주말 자유)
- **난이도**: 항상 Hard. 변경 시 반드시 질문 후 결정
- **문제 형식**: 게임 기획(디자인) 문서 / 기능 명세 / StS 기능 구현 맥락 / 변수명, 타입, 함수 시그니쳐 / 클래스 구조 등을 **절대** 정해주지 말고 사용자가 스스로 정하도록
- **목표**: 자립 가능한 1인 게임 개발자

---

## Gemini 커리큘럼(16주) 대비 변경 사항

| 변경 유형 | 내용 | 이유 |
|:--|:--|:--|
| **이동** | 프로퍼티 → 클래스 도입 시점(8주)으로 앞당김 | 클래스와 프로퍼티는 동시에 배워야 자연스러움 |
| **추가** | 디버깅 기초 (6주) | Gemini 커리큘럼에 완전 누락. 버그 못 잡으면 개발 불가 |
| **추가** | 구조체 + 값형/참조형 + 복사 (12주) | 카드 복제, 데미지 정보 등 게임 핵심 개념 |
| **추가** | 상속 vs 컴포지션 (10주) | Gemini는 상속만 강조. 유물/버프 시스템엔 컴포지션 필수 |
| **추가** | Nullable 타입 (16주) | null 참조 에러 = 게임 크래시 1순위 |
| **추가** | 네임스페이스 (8주) | 코드 규모 커지면 필수. POCU 코딩 표준에도 명시 |
| **추가** | 연산자 오버로딩, Object Initializer, enum Flags (21주) | 선택 사항이지만 실전에서 편의성 높음 |
| **분리** | 함수를 2주(기초+심화)로 확장 | Gemini는 1주에 압축. 오버로딩/out/ref는 별도 주 필요 |
| **확장** | 전체 기간 16주 → 22주 | 추가 개념 + 탄탄한 기초 확보. 완벽주의 성향 고려 |

---

## Phase 1: 기초 문법 완성 (1~6주)

Day 6까지 배운 것 위에 쌓는 단계. 배열 알고리즘부터 컬렉션, 함수까지.

### 1주차: 배열 심화 — 알고리즘 사고의 시작

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 배열 탐색: 최솟값/최댓값 찾기 (for + if 조합) | 가장 약한 적 자동 타겟팅 |
| 2일 | 인접 요소 처리: index-1, index+1 경계값 예외처리 | 스플래시 데미지, 범위 공격 |
| 3일 | 배열 요소 교환 (swap), 간단한 셔플 로직 | 덱 셔플링의 기초 |
| 4일 | 병렬 배열 복합 활용 (이름+체력+상태 동시 관리) | 전투 UI 상태창 구현 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | **연쇄 번개(Chain Lightning) 문제 도전** | Gemini가 출제한 Day 7 보스 챌린지 |

> 이 주의 핵심: "배열 문법을 아는 것"과 "배열로 로직을 짜는 것"은 다르다.
> 연쇄 번개를 힌트 없이 풀면 Phase 1의 1/3은 끝난 것.

---

### 2주차: 함수 기초 — 코드를 쪼개는 기술

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 함수 선언, 호출, 반환값 (void vs return) | `Attack()`, `Heal()` 기능 분리 |
| 2일 | 매개변수와 인자 전달 | `Attack(target, damage)` |
| 3일 | 반환값 활용: 함수 결과를 변수에 저장, 조건문에 활용 | `bool IsAlive()` 로 생존 판별 |
| 4일 | 지역 변수 vs 스코프: 함수 안에서 선언한 변수의 생명주기 | 반복문 밖에서 bool 선언 실수 방지 (기존 약점) |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 1주차 고블린 전투를 함수로 리팩토링 | 절차적 → 함수 기반 구조 전환 체험 |

---

### 3주차: 함수 심화 — 실전 패턴

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 함수 오버로딩 | `Attack(target)` vs `Attack(target, damage)` |
| 2일 | 기본값 매개변수 (POCU 표준: 0, null, false 사용) | `DrawCard(int count = 1)` |
| 3일 | out 매개변수 | `bool TryGetCard(out Card card)` 패턴 |
| 4일 | ref 매개변수 기초 (값 전달 vs 참조 전달 첫 접촉) | 체력 직접 수정 vs 복사본 수정 차이 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 전투 시스템에 함수 적용: 공격/방어/회복을 각각 함수로 분리 | 코드 구조화 실습 |

---

### 4주차: 컬렉션 1 — List와 foreach

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | List 선언, Add, Remove, Count, 인덱스 접근 | 덱(카드 뭉치) 관리의 시작 |
| 2일 | List와 foreach 반복문 | 손패의 모든 카드 순회 |
| 3일 | List 검색: Contains, IndexOf, Find | "덱에 특정 카드가 있는가?" |
| 4일 | List 정렬 기초: Sort (기본 정렬) | 인벤토리 정렬, 카드 비용순 나열 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 텍스트 덱 빌더: 초기 덱 생성 → 셔플 → 5장 뽑기 → 손패로 이동 | List 실전 활용의 핵심 |

---

### 5주차: 컬렉션 2 — Dictionary, Stack, Queue, HashSet

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | Dictionary: 키-값 쌍, 추가/조회/삭제 | 유물 이름으로 효과 찾기, 버프/디버프 관리 |
| 2일 | Dictionary 순회, ContainsKey, TryGetValue | 모든 버프 목록 출력, 안전한 조회 |
| 3일 | Stack (LIFO): Push, Pop, Peek | 뽑을 카드 더미(Draw Pile) |
| 4일 | Queue (FIFO), HashSet (중복 불가) | 턴 순서 큐, 중복 없는 유물 획득 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 컬렉션 종합: 덱(Stack) + 버린 카드(List) + 손패(List) + 유물(Dictionary) 통합 관리 시스템 | 4주차 덱빌더 확장 |

---

### 6주차: 유틸리티 + 디버깅 — 개발자의 도구

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | Enum: 선언, 사용, switch와 조합 (POCU: E 접두사) | `ECardType.Attack`, `ERarity.Rare` |
| 2일 | Assert (Debug.Assert): 가정 검증, 버그 조기 발견 | "데미지가 음수면 안 된다" 같은 가정 검증 |
| 3일 | StringBuilder: 긴 문자열 조합 최적화 | 전투 로그 텍스트 생성 |
| 4일 | **디버깅 기초**: Breakpoint 설정, Step Over/Into, Watch 창, 변수 값 실시간 확인 | **버그 못 잡으면 개발 불가. 가장 실용적인 스킬** |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 기존에 만든 전투/덱 시스템에 의도적 버그 삽입 → 디버거로 찾아 수정 | 디버깅 실전 훈련 |

> Phase 1 완료 기준: 배열 알고리즘, 함수, 컬렉션, 디버깅까지 소화.
> 여기까지 오면 "문법을 아는 단계"에서 "코드를 설계하는 단계"로 전환 준비 완료.

---

## Phase 2: 객체지향 (7~12주)

코드를 "기능별로 쪼개는" 단계에서 "개념별로 설계하는" 단계로.
StS의 카드, 몬스터, 유물 시스템의 뼈대를 만드는 구간.

### 7주차: 클래스 기초 — 데이터와 기능의 결합

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 클래스 선언, 멤버 변수, 인스턴스 생성 (new) | `Card` 클래스의 탄생 |
| 2일 | 생성자 (constructor), this 키워드 | `new Card("Strike", 1, 6)` |
| 3일 | 메서드: 클래스 안의 함수 | `card.Play()`, `monster.TakeDamage()` |
| 4일 | 클래스 vs 배열/병렬 배열: 왜 클래스가 필요한가 | 1주차 고블린 코드를 클래스로 재설계 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | `Monster` 클래스 설계: 이름, 체력, 공격력을 가진 몬스터 3종 생성 → List\<Monster\>에 담기 | 병렬 배열 → 클래스 전환 체험 |

---

### 8주차: 접근 제어 + 프로퍼티 + 네임스페이스

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 접근 제어자: public, private (POCU: m 접두사) | 외부에서 HP 직접 수정 방지 |
| 2일 | **프로퍼티 (get/set)**: 기본 사용법 | `public int HP { get; private set; }` |
| 3일 | **프로퍼티 심화**: set 내부 로직 (값 제한, 이벤트 연동 예고) | `set { mHp = Math.Max(0, value); }` |
| 4일 | **네임스페이스**: 코드 정리, 이름 충돌 방지 (POCU 표준) | `Cards.Attack.Strike` vs `Effects.Strike` |
| 5일 | 버퍼 / 보충 | — |
| 주말 | `Card` 클래스 완성: private 멤버 + 프로퍼티 + 네임스페이스 적용 | POCU 코딩 표준에 맞는 클래스 설계 |

---

### 9주차: 상속과 다형성 — 카드 시스템의 핵심

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 상속 기초: 부모/자식 클래스, base 키워드 | `Card` → `AttackCard`, `SkillCard` |
| 2일 | 오버라이딩 (virtual/override) | `AttackCard.Play()`와 `SkillCard.Play()`가 다르게 동작 |
| 3일 | 추상 클래스 (abstract): 미완성 틀 강제 | `abstract class Card { abstract void OnPlay(); }` |
| 4일 | 다형성 실전: `List<Card>`에 다양한 카드 담기, 반복문으로 일괄 처리 | 손패의 모든 카드를 종류 상관없이 Play() 호출 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | Backflip 코드 구조 따라 만들기: `CardModel` 상속받는 카드 3종 구현 | StS 실제 코드 패턴 체험 |

---

### 10주차: 컴포지션 + sealed — 설계 판단력

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | **상속의 한계**: 다중 상속 불가, 깊은 상속 트리의 문제점 | "공격이면서 방어인 카드"를 상속만으로는 못 만듦 |
| 2일 | **컴포지션**: "has-a" 관계, 효과 리스트로 기능 조합 | 카드에 `List<IEffect>` 달아서 효과 조합 |
| 3일 | 상속 vs 컴포지션: 언제 어떤 걸 쓰는가 | 카드 종류 = 상속, 카드 효과 = 컴포지션 |
| 4일 | sealed 클래스 (POCU: 최종 구현체 명시) | `sealed class Backflip : CardModel` |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 유물 시스템 설계: 컴포지션으로 "턴 시작 시 힘+1" 같은 유물 효과 구현 | 상속이 아닌 컴포지션이 적합한 대표 사례 |

---

### 11주차: 인터페이스 — 계약과 규격

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 인터페이스 선언, 구현 (POCU: I 접두사) | `IDamageable` 정의 |
| 2일 | 인터페이스 vs 추상 클래스: 차이와 사용 기준 | 다중 구현 가능 vs 단일 상속 |
| 3일 | 다중 인터페이스 구현 | `Monster : IDamageable, IBuffable` |
| 4일 | 인터페이스를 매개변수로: 타입에 의존하지 않는 코드 | `void DealDamage(IDamageable target)` |
| 5일 | 버퍼 / 보충 | — |
| 주말 | Player와 Monster 모두 IDamageable 구현 → 같은 함수로 공격 처리 | 다형성의 실전 활용 |

---

### 12주차: 구조체 + 값형/참조형 — 데이터의 본질

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | **구조체 (struct)**: 선언, class와의 차이 (POCU: S 접두사) | `SDamageInfo { int amount; bool isPiercing; }` |
| 2일 | **값형 vs 참조형**: stack vs heap, 복사 동작 차이 | 카드 데이터 넘길 때 원본이 바뀌는가? |
| 3일 | **얕은 복사 vs 깊은 복사** | 카드 복제 시 효과 리스트도 같이 복제되는가? |
| 4일 | **boxing/unboxing**, struct를 인터페이스에 넣을 때의 함정 | 성능과 의도치 않은 동작 방지 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | `SDamageInfo` 구조체 설계 + 전투 시스템에 적용: 데미지 양, 타입, 관통 여부를 하나로 묶기 | 값형이 적합한 사례 체험 |

> Phase 2 완료 기준: 클래스, 상속, 컴포지션, 인터페이스, 구조체까지 소화.
> 여기까지 오면 "카드 게임의 타입 시스템"을 직접 설계할 수 있는 상태.

---

## Phase 3: 고급 시스템 (13~18주)

개별 클래스를 넘어 시스템 간의 소통과 데이터 흐름을 다루는 단계.

### 13주차: static과 싱글턴 대안

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | static 멤버: static 변수, static 메서드 | `GameManager.TurnCount` |
| 2일 | static 클래스 (POCU: 싱글턴 대신 사용) | 유틸리티 함수 모음: `DamageCalculator.Calculate()` |
| 3일 | 왜 싱글턴을 피하는가 (POCU 표준 근거) | 전역 상태의 위험성, 테스트 어려움 |
| 4일 | static vs 인스턴스: 어떤 상황에서 어떤 걸 쓰는가 | 게임 설정 = static, 개별 카드 = 인스턴스 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | `GameSettings` static 클래스 + `CombatLog` static 유틸리티 구현 | 실전에서 static이 적합한 곳 |

---

### 14주차: 제네릭 — 타입을 변수로

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 제네릭 메서드: `T Max<T>(T a, T b)` | 타입 안전한 유틸리티 함수 |
| 2일 | 제네릭 클래스: `ObjectPool<T>` | 카드 풀, 이펙트 풀 |
| 3일 | 제네릭 제약 조건: `where T : IDamageable` | "데미지 받을 수 있는 것"만 허용 |
| 4일 | List\<T\>, Dictionary\<TKey, TValue\>의 내부 원리 재확인 | 이미 쓰고 있던 컬렉션이 제네릭이었다 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | `ObjectPool<T>` 구현: 카드 인스턴스를 재활용하는 풀 시스템 | 메모리 효율 + 제네릭 실전 |

---

### 15주차: 델리게이트와 이벤트 — 시스템 간 소통

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 델리게이트: 함수를 변수에 담기 | 카드 효과를 동적으로 교체 |
| 2일 | Action, Func: 미리 정의된 델리게이트 타입 | `Action<int> onDamageDealt` |
| 3일 | 이벤트 (event): 발행-구독 패턴 | "플레이어가 피해를 받았을 때" → 유물/버프가 반응 |
| 4일 | 이벤트 체인: 여러 구독자가 순서대로 반응 | StS의 핵심: 카드 → 유물 → 버프 연쇄 반응 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 이벤트 기반 전투 시스템: `OnTurnStart`, `OnDamageDealt`, `OnCardPlayed` 이벤트 구현 | StS 게임 루프의 핵심 구조 |

---

### 16주차: Nullable + 예외 처리 기초

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | **Nullable 값 타입**: `int?`, `HasValue`, `Value` | 아직 결정되지 않은 타겟, 비어있는 슬롯 |
| 2일 | **null 조건 연산자**: `?.` (POCU: `??`는 금지) | 안전한 null 체크 체인 |
| 3일 | **예외 처리 기초**: try-catch-finally | 파일 로드 실패, 잘못된 입력 처리 |
| 4일 | **POCU 예외 철학**: 경계에서만 처리, 내부에서 던지지 않기 | 외부 데이터(세이브 파일) vs 내부 로직 분리 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 세이브 파일 로드 시뮬레이션: 존재하지 않는 파일, 손상된 데이터 처리 | 예외 처리 실전 |

---

### 17주차: LINQ — 데이터 질의

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | LINQ 기초: Where, Select, OrderBy | 덱에서 공격 카드만 필터링 |
| 2일 | LINQ 집계: Count, Sum, Average, Min, Max | 덱 통계: 평균 코스트, 총 공격력 |
| 3일 | LINQ 고급: GroupBy, FirstOrDefault, Any, All | 카드 타입별 그룹핑, "방어 카드가 있는가?" |
| 4일 | LINQ vs 반복문: 가독성과 성능 트레이드오프 | 언제 LINQ를 쓰고, 언제 for문을 쓰는가 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 덱 분석기: LINQ로 덱 통계, 추천 카드, 밸런스 경고 출력 | LINQ 종합 활용 |

---

### 18주차: 비동기 기초 — 멈추지 않는 프로그램

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | async/await 기본 개념: 왜 비동기가 필요한가 | 세이브하는 동안 게임이 멈추면 안 됨 |
| 2일 | Task, Task\<T\>: 비동기 작업의 단위 (POCU: async Task 사용) | `async Task SaveGame()` |
| 3일 | 파일 I/O 비동기: ReadAllTextAsync, WriteAllTextAsync | 세이브/로드 비동기 처리 |
| 4일 | 비동기 예외 처리 + 취소 토큰 (CancellationToken) | 로드 중 취소, 타임아웃 처리 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 비동기 세이브/로드 시스템: JSON 직렬화 + 파일 비동기 I/O | Phase 3 마무리 실습 |

> Phase 3 완료 기준: 제네릭, 이벤트, LINQ, 비동기까지 소화.
> 여기까지 오면 "이벤트 기반 게임 루프"를 설계하고 구현할 수 있는 상태.

---

## Phase 4: 통합 (19~22주)

배운 모든 것을 하나의 프로젝트로 합치는 단계.

### 19주차: 파일 I/O + JSON 직렬화

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 파일 읽기/쓰기: File.ReadAllText, File.WriteAllText | 세이브 파일 기본 |
| 2일 | JSON 직렬화: System.Text.Json (직렬화/역직렬화) | 게임 상태를 JSON으로 저장 |
| 3일 | JSON 커스텀 설정: 속성 이름 매핑, null 처리, enum 변환 | 세이브 파일 포맷 설계 |
| 4일 | 세이브 시스템 설계: 무엇을 저장하고 무엇을 저장하지 않는가 | 덱, 유물, 현재 층, HP를 JSON으로 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 전투 상태 저장/불러오기: 전투 중간에 저장했다가 이어하기 | 직렬화 실전 |

---

### 20주차: 에러 처리 심화 + 로깅

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | 커스텀 예외 클래스: `InvalidCardException` | 게임 특화 에러 정의 |
| 2일 | 예외 계층 설계: 어디서 잡고, 어디서 전파하는가 | UI 레이어 vs 로직 레이어 분리 |
| 3일 | 로깅 시스템: 간단한 Logger 구현 | 전투 로그, 디버그 로그 분리 |
| 4일 | Debug.Assert vs 예외 vs 로그: 3가지 도구의 사용 기준 | POCU 철학 정리 |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 기존 전투 시스템에 에러 처리 + 로깅 적용 | 안정성 강화 |

---

### 21주차: 선택 사항 + 종합 복습

| 일차 | 학습 내용 | StS 연관성 |
|:--|:--|:--|
| 1일 | **연산자 오버로딩**: `+`, `==`, `>` 재정의 | `card1 > card2` 로 비교 |
| 2일 | **Object Initializer**: `new Card { Name = "Strike", Cost = 1 }` | 빠른 초기화 문법 |
| 3일 | **enum Flags (비트 플래그)**: `[Flags] enum ECardTag` (POCU: Flags 접미사) | 카드에 여러 태그 동시 부여 |
| 4일 | 종합 복습: Phase 1~3 핵심 개념 퀴즈 | — |
| 5일 | 버퍼 / 보충 | — |
| 주말 | 종합 프로젝트 설계 문서 작성: 어떤 클래스, 어떤 인터페이스, 어떤 이벤트를 쓸 것인가 | 22주차 프로젝트 준비 |

---

### 22주차: 종합 프로젝트 — 텍스트 기반 미니 StS

> 종합 프로젝트 최소 사양:
> - 카드 3종 이상 (공격, 방어, 스킬)
> - 몬스터 2종 이상
> - 턴 시스템 (플레이어 → 적)
> - 이벤트 기반 버프/디버프 1개 이상
> - 세이브/로드
> - POCU 코딩 표준 준수

---

## 전체 요약

| Phase | 기간 | 핵심 | 완료 후 할 수 있는 것 |
|:--|:--|:--|:--|
| 1. 기초 문법 | 1~6주 | 배열, 함수, 컬렉션, 디버깅 | 절차적 코드로 전투 시스템 구현 |
| 2. 객체지향 | 7~12주 | 클래스, 상속, 컴포지션, 인터페이스, 구조체 | 카드/몬스터/유물의 데이터 구조 설계 |
| 3. 고급 시스템 | 13~18주 | 제네릭, 이벤트, LINQ, 비동기 | 이벤트 기반 게임 루프, 카드 효과 시스템 |
| 4. 통합 | 19~22주 | 예외처리, JSON, 종합 프로젝트 | 텍스트 기반 미니 StS 완성 |

**총 22주 (약 5.5개월)**
Gemini 16주 대비 6주 추가. 추가 개념 10개 + 함수 심화 분리 + 종합 프로젝트 포함.

---

## 학습 규칙

1. **주간 목표제**: 주 5개념 목표, 어느 날 하든 상관없음. 못 한 날 = 실패 아님.
2. **난이도**: 항상 Hard. 변경은 반드시 물어본 후.
3. **진도**: 반드시 사용자가 넘어가자고 할 때만 다음 일차로 / 개념 설명 후 개념 이해를 확인 받은 뒤에 문제 내기.
4. **피드백**: QA 버그 리포트 형식. 힌트 절대 금지.
5. **코딩 표준**: POCU C# 코딩 표준 적용.
6. **감정 관리**: 자기비하 표현 시 감정 공감 후 현실적 조언.
